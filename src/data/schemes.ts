import { Scheme } from '../types';
import { STATE_SCHEMES } from './stateSchemes';

export { STATE_SCHEMES };

// SCHEMES_DATABASE exclusively consists of State Government Schemes (Andhra Pradesh)
export const SCHEMES_DATABASE: Scheme[] = [...STATE_SCHEMES];
